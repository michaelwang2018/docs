---
title: "Get Conversation"
api: "GET /api/v1/conversations/{conversation_id}"
description: "Retrieve detailed information about a specific conversation"
---

## Get Conversation

Get comprehensive details about a specific conversation including its metadata, settings, and status.

### Headers

<ParamField header="Authorization" type="string">
  Bearer token for authentication
</ParamField>

<ParamField header="X-API-Key" type="string">
  API key for authentication (alternative to Authorization header)
</ParamField>

<ParamField header="X-Workspace-Id" type="string">
  Optional. Derived from API Key, but can be specified if you have access to multiple workspaces
</ParamField>

### Path Parameters

<ParamField path="conversation_id" type="string" required>
  The unique identifier of the conversation to retrieve
</ParamField>

### Response

<ResponseField name="id" type="string">
  Unique conversation identifier
</ResponseField>

<ResponseField name="title" type="string">
  Conversation title
</ResponseField>

<ResponseField name="summary" type="string">
  Auto-generated conversation summary
</ResponseField>

<ResponseField name="status" type="string">
  Conversation status (active, ended, archived)
</ResponseField>

<ResponseField name="is_public" type="boolean">
  Whether the conversation is public
</ResponseField>

<ResponseField name="collections" type="array">
  Array of collection IDs associated with this conversation
</ResponseField>

<ResponseField name="meta_data" type="object">
  Additional conversation metadata
</ResponseField>

<ResponseField name="settings" type="object">
  Conversation-specific settings and configuration
</ResponseField>

<ResponseField name="ended_at" type="string">
  Timestamp when conversation was ended (null if still active)
</ResponseField>

<ResponseField name="owner_id" type="string">
  ID of the conversation owner
</ResponseField>

<ResponseField name="workspace_id" type="string">
  ID of the workspace this conversation belongs to
</ResponseField>

<ResponseField name="created_by" type="string">
  ID of the user who created the conversation
</ResponseField>

<ResponseField name="created_at" type="string">
  Timestamp when conversation was created
</ResponseField>

<ResponseField name="updated_at" type="string">
  Timestamp when conversation was last updated
</ResponseField>

<RequestExample>

```bash cURL
curl "https://api.mielto.com/api/v1/conversations/conv_123456789" \
  -H "X-API-Key: YOUR_API_KEY"
```

```python Python
import requests

conversation_id = "conv_123456789"
response = requests.get(
    f"https://api.mielto.com/api/v1/conversations/{conversation_id}",
    headers={"X-API-Key": "YOUR_API_KEY"}
)

conversation = response.json()
```

```javascript JavaScript
const conversationId = "conv_123456789";
const response = await fetch(`https://api.mielto.com/api/v1/conversations/${conversationId}`, {
  headers: {
    "X-API-Key": "YOUR_API_KEY"
  }
});

const conversation = await response.json();
```

</RequestExample>

<ResponseExample>

```json Response
{
  "id": "conv_123456789",
  "title": "Product Strategy Discussion",
  "summary": "Discussion about Q2 product strategy and key market considerations focusing on competitive analysis and customer needs",
  "status": "active",
  "is_public": false,
  "collections": ["col_123456789", "col_987654321"],
  "meta_data": {
    "category": "planning",
    "priority": "high",
    "department": "product",
    "tags": ["strategy", "q2", "planning"]
  },
  "settings": {
    "auto_summarize": true,
    "context_window": 4000,
    "max_tokens": 2000,
    "temperature": 0.7
  },
  "ended_at": null,
  "owner_id": "usr_123456789",
  "workspace_id": "wsp_123456789",
  "created_by": "usr_123456789",
  "created_at": "2024-01-16T10:00:00Z",
  "updated_at": "2024-01-16T15:30:00Z"
}
```

</ResponseExample>

<Note>
  To retrieve messages within this conversation, use the [Get Conversation Messages](/api-reference/conversations/messages) endpoint.
</Note>