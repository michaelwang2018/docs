---
title: "Get Workspace"
api: "GET /api/v1/workspaces/{workspace_id}"
description: "Get detailed information about a specific workspace"
---

## Get Workspace

Retrieve detailed information about a specific workspace by ID.

### Headers

<ParamField header="X-API-Key" type="string" required>
  Your API key for authentication
</ParamField>

<ParamField header="X-Workspace-Id" type="string">
  Optional workspace ID header
</ParamField>

### Path Parameters

<ParamField path="workspace_id" type="string" required>
  The unique identifier of the workspace to retrieve
</ParamField>

### Response

<ResponseField name="id" type="string">
  Unique workspace identifier
</ResponseField>

<ResponseField name="name" type="string">
  Workspace name
</ResponseField>

<ResponseField name="slug" type="string">
  URL-friendly workspace identifier
</ResponseField>

<ResponseField name="description" type="string">
  Workspace description
</ResponseField>

<ResponseField name="status" type="string">
  Workspace status (active, inactive, suspended)
</ResponseField>

<ResponseField name="tier_id" type="string">
  Reference to workspace tier
</ResponseField>

<ResponseField name="logo_url" type="string">
  URL to workspace logo
</ResponseField>

<ResponseField name="settings" type="object">
  Workspace configuration settings
</ResponseField>

<ResponseField name="billing" type="object">
  Billing information and settings
</ResponseField>

<ResponseField name="created_at" type="string">
  Workspace creation timestamp
</ResponseField>

<ResponseField name="updated_at" type="string">
  Last update timestamp
</ResponseField>

<ResponseField name="created_by" type="string">
  ID of user who created the workspace
</ResponseField>

<RequestExample>

```bash cURL
curl -X GET "https://api.mielto.com/api/v1/workspaces/wsp_123456789" \
  -H "X-API-Key: YOUR_API_KEY"
```

```python Python
import requests

workspace_id = "wsp_123456789"
response = requests.get(
    f"https://api.mielto.com/api/v1/workspaces/{workspace_id}",
    headers={"X-API-Key": "YOUR_API_KEY"}
)

workspace = response.json()
```

```javascript JavaScript
const workspaceId = "wsp_123456789";
const response = await fetch(`https://api.mielto.com/api/v1/workspaces/${workspaceId}`, {
  headers: {
    "X-API-Key": "YOUR_API_KEY"
  }
});

const workspace = await response.json();
```

</RequestExample>

<ResponseExample>

```json Response
{
  "id": "wsp_123456789",
  "name": "My Workspace",
  "slug": "my-workspace",
  "description": "Main workspace for AI projects",
  "status": "active",
  "tier_id": "tier_free",
  "logo_url": "https://api.mielto.com/logos/wsp_123456789.png",
  "settings": {
    "memory": {
      "max_memories": 10000,
      "auto_cleanup": true,
      "retention_days": 30
    },
    "model": {
      "provider": "openai",
      "model": "gpt-4o"
    }
  },
  "billing": {
    "plan": "free",
    "usage": {
      "api_calls": 150,
      "storage_mb": 250
    }
  },
  "created_at": "2024-01-15T10:00:00Z",
  "updated_at": "2024-01-15T10:30:00Z",
  "created_by": "usr_123456789"
}
```

</ResponseExample>